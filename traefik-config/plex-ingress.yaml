apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: plex-dashboard
  namespace: traefik
spec:
  routes:
  - match: Host(`plex.internal`)
    kind: Rule
    services:
    - name: plex
      kind: TraefikService
      namespace: traefik
      port: 32400



